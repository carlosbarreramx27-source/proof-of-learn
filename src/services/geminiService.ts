export function generateChatMessage(message: string): string {
    // Function to generate chat message
    return `Chat Message: ${message}`;
}

export function generateImage(imageUrl: string): string {
    // Function to generate image representation
    return `Image URL: ${imageUrl}`;
}